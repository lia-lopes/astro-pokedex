---
interface Props {
  name: string;
  id: number;
}

const { name, id } = Astro.props;
---

<h2 class="capitalize w-fit mb-2 text-2xl flex flex-row flex-wrap gap-x-2 items-center">
  <span class="w-max">{name.replaceAll("-", " ")}</span>
  <span class="italic">#{id}</span>
</h2>
