info:
  name: Species
  type: http
  seq: 2

http:
  method: GET
  url: https://pokeapi.co/api/v2/pokemon-species?limit=10000
  params:
    - name: limit
      value: "10000"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
